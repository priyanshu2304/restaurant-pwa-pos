export default function CreditNotes() {
  return (
    <div style={{ padding: 16 }}>
      <h2>Credit Notes (Stub)</h2>
      <p>Finalized invoices are immutable. To reverse, issue a credit note referencing the invoice number.</p>
      <p>Stub only (server endpoint <code>POST /credit-notes</code> exists). Implement your business rules later.</p>
    </div>
  );
}
